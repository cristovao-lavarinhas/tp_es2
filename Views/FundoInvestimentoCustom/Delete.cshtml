@* Views/FundoInvestimentoCustom/Delete.cshtml *@
@model esii.Models.FundoInvestimentoCreateViewModel
@{
    ViewData["Title"] = "Eliminar Fundo";
}

<div class="delete-container">
    <h2 class="delete-title">@ViewData["Title"]</h2>
    <dl class="row">
        <dt class="col-sm-4">Nome</dt>
        <dd class="col-sm-8">@Model.Nome</dd>
        <dt class="col-sm-4">Montante Investido</dt>
        <dd class="col-sm-8">@Model.MontanteInvestido.ToString("C")</dd>
    </dl>
    <form asp-action="Delete" asp-route-id="@Model.AtivoId" method="post" onsubmit="return confirm('⚠ Esta ação é irreversível. Tens a certeza?');">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="AtivoId" />
        <div class="btn-group">
            <button type="submit" class="btn btn-danger">
                <i class="bi bi-trash"></i> Eliminar
            </button>
            <a asp-action="Index" class="btn btn-secondary">
                <i class="bi bi-arrow-left-circle"></i> Cancelar
            </a>
        </div>
    </form>
</div>

